module.exports = {
  root: true,
  extends: '@react-native',
  rules: {},
};

/*

  'no-console': 'warn',
  // 코드에서 console.log와 같은 문장을 허용하지 않음

  'semi': ['error', 'always'],
  // 세미콜론을 항상 사용하도록 강제함

  'react/jsx-no-duplicate-props': ['error'],
  // 중복된 props를 허용하지 않음

  'react/jsx-uses-vars': 'error',
  // JSX 안에서 사용되는 변수들은 반드시 정의되어 있어야 함

  'no-unused-vars': 'warn',
  // 미사용 변수에 대한 경고

  'react-hooks/rules-of-hooks': 'error',
  // 훅의 규칙을 강제함

  'react-hooks/exhaustive-deps': 'warn',
  // 훅의 의존성 배열에 빠진 값이 있는지 검사

  'jsx-quotes': ['error', 'prefer-double'],
  // JSX 파일에서 단일 인용부호(') 대신 이중 인용부호(") 사용

  'no-useless-computed-key': 'warn',
  // 객체 리터럴에서 계산된 속성을 강제하지 않음

  'object-curly-spacing': ['error', 'always'],
  // 배열과 객체 리터럴에서 괄호 안에 공백 사용을 강제함

  'array-bracket-spacing': ['error', 'never'],
  'arrow-parens': ['error', 'as-needed'],
  // 화살표 함수 괄호 사용 방식

  'max-classes-per-file': ['error', 1],
  // 파일당 최대 클래스 개수를 한 개로 제한

  'import/order': 'warn'
  // import 순서를 강제


  */
